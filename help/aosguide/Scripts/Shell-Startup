Prompt "%N.%S> "
Alias CLS Echo "*E[0;0H*E[J" NOLINE
Alias XCopy Copy CLONE
Alias Exit EndShell
Alias NS NewShell
Alias EdN Run Ed RAM:NewFile Window=CON:50/50/400/400/Editor
Alias qDir Dir ~(#?.info)
Alias qList List Files ~(#?.info)

;     CLS    löscht in diesem Fall das Shell-Fenster
;     XCopy  erstellt geklonte Kopien
;     Exit   beendet die Shell
;     NS     öffnet eine neue Shell
;     EdN    öffnet das »ED« Fenster mit einer bestimmten Größe
;            und erstellt im RAM: die neue Datei »NewFile«
;     qDir   zeigt nur Verzeichnisse
;     qList  zeigt nur Dateien.


