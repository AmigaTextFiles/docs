                          --AMIGADOS GUIDE V2.6--
                                   ENDIF


   USE:
   ENDIF is used to terminate an IF construct.

   PATH:
   Internal

   SYSTEM:
   2.0 (enhanced), 2.1, 3.0

   EXAMPLES:
   1>ENDIF

   NOTES:
   Below is an example of how the IF....ENDIF process works.

   IF WARN

   ECHO "A warning condition has occurred"

   ENDIF


   ENDIF may only be used in script files.
