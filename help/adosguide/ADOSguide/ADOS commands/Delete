                          --AMIGADOS GUIDE V2.6--
                                  DELETE


   USE:
   Delete attempts to erase any files or directories which you have
   specified.  This command should be used with caution.

   PATH:
   C:

   SYSTEM:
   2.0 (enhanced), 2.1, 3.0

   EXAMPLES:
   1>DELETE File1
   1>DELETE DF0:#?Doc#? (deletes every file ending in "Doc")
   1>DELETE RAM: ALL (deletes all files in Ram)

   NOTES:
   Make sure that you understand what you have told the computer to delete
   because you will not be able to get it back once deleted.

   It is possible to abort the Delete process once it has started by using
   the Ctrl+C break combination.
