; Example-28.1

LAB Start
ASK "Put disk to test in df0: then press RETURN"
IF EXISTS df0:s/startup-sequence file
  ECHO "Disk has a startup-file"
  ASK "Shall I type the file? (Y/N)"
  IF NOT WARN
    SKIP Finish
  ELSE
    TYPE df0:s/startup-sequence
  ENDIF
ELSE
  ECHO "Disk has NO startup-sequence"
ENDIF
LAB Finish
ASK "Test another disk? (Y/N)"
IF WARN
  SKIP Start BACK
ENDIF
