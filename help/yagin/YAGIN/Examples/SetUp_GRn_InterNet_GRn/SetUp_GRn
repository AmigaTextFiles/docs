; Setup for GRN   (run setup for UUCP and INet first!)

; $VER: SetUp_GRn 1.0 (5.7.95) Arndt van der Molen

; No need to setup. Use setup from UUCP!
;SetEnv UULIB			see UUCP

; No need to setup. Use setup from UUCP!
;SetEnv UUNEWS			see UUCP

; No need to setup. Use setup from UUCP!
;SetEnv UUSPOOL			see UUCP

SetEnv NEWSEDITOR		edc -sticky
SetEnv MAILEDITOR		edc -sticky
SetEnv SENDMAIL			$SMTPPOST                 ; Use setup from INet-Utils
SetEnv POSTNEWS			UUCP:c/PostNews           ; Use NewPostNewsSpool
SetEnv GRNSAVEDIR		ram:
