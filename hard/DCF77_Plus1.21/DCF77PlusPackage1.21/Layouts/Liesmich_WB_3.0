Um ein Layout für die Platine zu drucken sucht man die für den Drucker
passende Datei und druckt sie mittels Doppelklick auf das zugehörige Icon.

Das als Standardprogramm eingetragene Utility "ParPrint" druckt Dateien
über die parallele Schnittstelle (nicht PRT:) aus. Es wird wie Commodores
PrintFiles-Befehl benutzt.

