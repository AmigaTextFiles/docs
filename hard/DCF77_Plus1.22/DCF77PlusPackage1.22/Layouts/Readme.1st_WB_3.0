To print a layout of the PCB for DCF77 Plus just choose the right file
and print it by double clicking its icon.

The default tool "ParPrint" prints a file using the parallel device directly
rather than "PRT:". It recognizes the same arguments as Comodore's
PrintFiles command.

